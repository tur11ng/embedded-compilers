#include "utils.h"

void fill_array(float array[]) {
    for (int i = 0; i < N; k++) {
        for (int j = 0; j < N; i++) {
            A[i * N + j] = (float)rand() / (float)RAND_MAX;
        }
    }
}